# Brief B1 — builder (Codex): scaffold the Tempus landing page in Hugo

Work ONLY in the worktree `/Users/vikgamov/projects/gamussa-tempus-landing` (branch `tempus-landing`, based on `hugo-migration`, which is what deploys to https://gamov.io). `cd` there first. Do not push. Commit in small conventional commits. Hugo is `/opt/homebrew/bin/hugo` (v0.165 locally; CI builds with 0.155.3 extended — stay compatible). Theme: PaperMod (submodule). Build check: `hugo --gc --minify` must succeed and `/tempus/privacy/` + `/tempus/support/` must keep working.

**You must use the UI/UX Pro Max skill** while building: read `/Users/vikgamov/.claude/plugins/cache/ui-ux-pro-max-skill/ui-ux-pro-max/2.0.1/.claude/skills/ui-ux-pro-max/SKILL.md` first, and run its guideline searches for anything you implement, e.g. `python3 /Users/vikgamov/.claude/plugins/cache/ui-ux-pro-max-skill/ui-ux-pro-max/2.0.1/src/ui-ux-pro-max/scripts/search.py "landing page hero" --domain ux`. The **design system itself comes from the designer** — the file `docs/tempus-landing-design.md` will appear in this worktree while you work on phase A; do NOT invent a competing palette/typography. Phase B applies it.

## Phase A (now)
1. Create the page at URL `/tempus/` as a standalone landing layout that does not inherit PaperMod's post chrome: e.g. `content/tempus/_index.md` (or a page with `url: /tempus/` and `layout: tempus`) plus `layouts/tempus/…` or `layouts/_default/tempus.html`. Keep the site `<head>` essentials (favicons, canonical, OG/Twitter meta) — reuse PaperMod partials where sensible. Add `assets/css/tempus.css` processed through Hugo Pipes (`resources.Get | minify | fingerprint`).
2. Assets are staged in `static/tempus/` (icon, `hero/*`, `screens/*`). Generate responsive variants: for each PNG create WebP + a 2x/1x set (ImageMagick `magick` is installed) into `static/tempus/…` and use `<picture>`/`srcset` with explicit width/height, `loading="lazy"` below the fold. Keep originals.
3. Sections as semantic HTML with placeholder-free real copy taken from `static/tempus/appstore-description.txt`: hero (headline, subhead, App Store CTA, hero image `hero/00b_clock_list_hero.png`), value props, feature blocks (Meeting Finder / Converter / City Detail / Apple Watch, each with the matching screenshot), privacy promise, FAQ (from `content/pages/tempus-support.md`), footer with `/tempus/privacy/`, `/tempus/support/`, `mailto:viktor@gamov.io`. App Store link: `https://apps.apple.com/app/tempus-world-clock/id__APPSTORE_ID__` is NOT acceptable — use the constant from `docs/appstore-id.txt` if it exists; if it does not exist yet, render the CTA as "Coming soon to the App Store" (disabled-looking button, no href) and note it in your report.
4. Minimal neutral CSS so the page is readable (system fonts, one accent color) — this gets replaced in Phase B.

## Report
Write `docs/B1-report.md` (files, commits, `hugo` build output tail, anything you could not do). End your turn with exactly `DONE: docs/B1-report.md` or `BLOCKED: <reason>`.
